# 性能分析 Playbook

从 bpftime ssl-nginx 调查战役（2026-07）提炼的可复用方法论。每条原则附本次
战役中的真实案例作对照。适合作为下一个性能项目的入手 checklist。

## 阶段 0：先证明测量可信（否则后面全白干）

- [ ] **方差是报警器**：CV 异常大、双峰分布、同配置结果跳变 → 先怀疑测量而非
  性能。*案例：bpftime CV 8.7–16.6% 的双峰最终定位为 perf record 对齐 bug +
  共享内存残留——早期"bpftime 更快"的数据全是丢事件换来的假象。*
- [ ] **建立清态协议**：每轮测量前把系统恢复到已知状态，并写进脚本强制执行。
  *案例：`bpftimetool remove` 清共享内存成为每轮必做；残留状态曾造成 13–14k
  RPS 的虚高。*
- [ ] **守恒审计**：任何"变快了"都要证明不是靠丢活换的——找一个守恒量对账。
  *案例：sslsniff 收到的事件数必须 ≈ 2 × wrk 完成请求数；no-affinity 变体
  以 2.001 事件/请求零丢失过审。*
- [ ] **环境即数据**：记录主频/温度/电源模式/时区/构建选项；引用任何历史数据前
  **核对它当时真实运行的代码**。*案例：07-23/24 的 CI run checkout 的分支 HEAD
  带着消融版 BPF 程序，差点被当成正常基线引用；CI 与本地的
  PROBE_READ_CHECK 配置曾不一致。*

## 阶段 1：定位（把大数字拆成小数字）

- [ ] **消融分解**：凡是能逐段关掉的系统，就逐段关掉测差值。整体数字（"慢
  36%"）没有行动价值，分段数字（"④段占 17.5pp"）自带靶心。*案例：V1–V4
  四阶段消融 + ④段内部再消融，两轮就从"bpftime 慢"走到"这 10 行绑核代码"。*
- [ ] **同轮归一化**：impact 一律相对同一轮的 baseline 计算，消除跨日漂移；
  比较用差值（pp）不用绝对值。
- [ ] **噪声水位先行**：先弄清楚测量自身的抖动幅度（±几 pp），小于噪声的差异
  一律不解读。*案例：kernel 在 V2→V3 出现 −2.8pp 的"负成本"，恰好标定了
  噪声带；±2-3pp 内的 ②③ 排序从不下结论。*

## 阶段 2：归因（从"哪一段"到"哪一行"）

- [ ] **机制必须在量级上闭环**：每事件成本 × 事件数 ÷ 请求预算 ≈ 观测 pp，
  账算平才算找到了真原因；对不上就是还有隐藏事实。*案例：按 6 核摊算对不上
  → 逼出 `worker_processes 1`（单 worker 是瓶颈）这一关键配置；6.3µs/请求 ÷
  60µs 预算与 ④段观测吻合后才收口。*
- [ ] **微基准验证每个假设**：两分钟的实测碾压半小时的架构推理。*案例：
  `sched_getcpu` 实测 3.5ns（rseq）当场证伪"ARM 缺 vDSO 所以贵"的推断；
  setaffinity 1.4µs 坐实真凶。*
- [ ] **代码走读要回答"它保护了什么"**：对可疑的昂贵操作，逐一列举它可能保护
  的对象并逐一排除。*案例：绑核序列的三连问——lookup key？（快照先于绑核）
  写入安全？（per-thread shard）互斥？（绑核非锁）——三个都不是，判冗余。*
- [ ] **预测写下来再验证**：被打脸的预测是最高质量的信息。*案例：「x64 上删
  绑核只值 2–4pp」的书面预测被同 VM A/B（+37–41%）证伪，直接把结论从
  「ARM 特有」升级为「跨平台手刹」。*

## 阶段 3：修复与验证（让结论无可辩驳）

- [ ] **判定阈值预注册**：跑实验之前写下"结果为 X 则结论 A、为 Y 则结论 B"，
  防止事后解释。*案例：PREREGISTRATION.md 的 ≥10pp/4–10pp/<4pp 三档。*
- [ ] **认出白捡的对照组**：系统里与被测代码无关的部分就是免费对照。*案例：
  kernel 腿不含 bpftime 代码，其 A/B Δ（≤0.7%）即同 VM 噪声水位；首尾各跑
  一次 original 即免费漂移检测（bookend）。*
- [ ] **小效应必须同环境 A/B**：跨 run/跨机器的对比会被环境方差淹没（云 runner
  硬件抽签下 baseline 可差 30%+）；比值类指标（A/B 两腿同 run）对抽签免疫。
- [ ] **多口径交叉验证**：同一结论用独立路径再测一遍。*案例：短测的 noop 变体
  收回 17.51pp 与完整消融的 V4−V3=17.5pp 精确互证；fair 口径与 stock 口径
  同向。*
- [ ] **修复后回看方差**：真修复通常同时收敛方差。*案例：删绑核后 CV 从
  ~5% 收敛到 kernel 水平，离群消失——因为子 bug（钉核泄漏、mask 竞态）正是
  抖动来源。*

## 阶段 4：记录与交付

- [ ] **文档分层**：一页纸根因报告（入口）→ 修复清单（bug 台账）→ 详细分析
  （归档）→ 操作手册（复现）。读者按需下钻。
- [ ] **错误留痕**：撤回的数据、被证伪的预测、走过的弯路都写进更新记录——
  可信度来自可审计性，不来自无错。*案例：x64 章节三次改写全部留痕。*
- [ ] **证据强度分级**：每个结论标注它的证据等级（n=1 方向性 / 单轮 / 多轮
  预注册 / 同环境 A/B），不同强度的结论不混着说。
- [ ] **趁热上游化**：修复不推上游，代码一漂移证据就要重对齐。

## 十秒钟版本

> 先证明测不准的都修了；把大数字消融成小数字；对最大的小数字问"它保护了
> 什么"；预测写下来、用微基准和同环境 A/B 打自己的脸；账在量级上算平才收口；
> 一切留痕。

## 本战役索引

方法论的完整实例见本目录：`bpftime-arm-performance-root-cause-20260727.md`
（结果）与 `archive/`（全过程）。
